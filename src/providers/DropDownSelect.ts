export class DropDownSelect {
    //extends Filtro {


    private _manipulated: any;

    private _lista: any[];

    public toGenViewSelects(objectToViewSelects): void {

    }

    onChange() {

        for (let i in this._manipulated) {
            if (i && typeof i !== 'function') {
                
            }
        }
    }



}

